---
"react-modern-audio-player": patch
---

Hovering a `useAudioPlayer*` hook in your editor now shows which part of the player state it subscribes to, so you can tell which hook re-renders your component on every playback tick and which one stays quiet.
