import { FC } from "react";
import styled from "styled-components";

const TrackInfoContainer = styled.div``;

export const TrackInfo: FC = () => {
  return <TrackInfoContainer>trackInfo</TrackInfoContainer>;
};
